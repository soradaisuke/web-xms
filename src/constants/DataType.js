const NUMBER = 'number';
const STRING = 'string';
const DATETIME = 'datetime';
const ORDER = 'order';
const IMAGE = 'image';
const ENUM = 'enum';
export default {
  NUMBER,
  STRING,
  DATETIME,
  ORDER,
  IMAGE,
  ENUM,
};
